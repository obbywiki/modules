# TODO

This is a top-level TODO, mainly for new templates or modules that need to be made/implemented. If you have a task for an existing element, add TODO.md under its directory instead.

* A template for other templates to invoke that adds a category only if the page it is rendered on is not a template or module.
* A template for better template syntax highlighting, for use in template documentation.